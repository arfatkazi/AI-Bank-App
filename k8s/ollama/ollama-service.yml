kind: Service
apiVersion: v1


metadata:
  name: ollama-service
  metadata: bankapp
  labels:
    app: ollama

spec:
  selector:
    metadata:
      app: ollama
    ports:
      - name: ollama
        port: 11434
        targetPort: 11434
    type: ClusterIP 

